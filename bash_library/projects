if [ ! -z "$BASHPROJECTS" ];
then
    for f in $BASHPROJECTS/*;
    do
        source $f;
    done
fi

[ "$NOBANNER" = "false" ] && echo "Projects:"
[ "$NOBANNER" = "false" ] && echo "$HELP_PROJECTS"
[ "$NOBANNER" = "false" ] && echo ""
